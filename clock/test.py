import sqlite3
import datetime
from PySide6.QtCore import *
from mytime import MyTime
from win10toast import ToastNotifier
from database import Database


# db = Database()
# i = db.get_alarms()
# for j in range(len(i)):
#     k = str(i[j][2]).split(":")
#     print(k[0])
print(datetime.datetime.now())
# con = sqlite3.connect('alarm.db')
# cursor = con.cursor()
# for i in :
#     result = cursor.execute(f'SELECT time FROM alarm')
#     alarm_time = result.fetchone()
# o = str(alarm_time[0]).split(":")
# print(o)

